<div class="row">
    {{#if model}}
        {{#each model as |item|}}
            <div class="card-container col-xl-3 col-lg-4 col-md-6 col-sm-12">
                {{component 'movie-card' item=item onclick='clickHandler'}}
            </div>
        {{/each}}
    {{/if}}
</div>
